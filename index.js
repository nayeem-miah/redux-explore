// state
const initialCounterState = {
    count: 0
};
const initialUsersState = {
    users: [{ name: "Nayeem" }]
};

// action ---> type , payload

{
    type: "INCREMENT"
}
// INCREMENT COUNTER
// DECREMENT COUNTER